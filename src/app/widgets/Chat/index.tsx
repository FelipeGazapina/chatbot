import ChatHistory from "./ChatHistory";
import ChatRoot from "./ChatRoot";
import ChatSearch from "./ChatSearch";

export const Chat = {
    Root: ChatRoot,
    Search: ChatSearch,
    History: ChatHistory,
}